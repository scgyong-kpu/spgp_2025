<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!--
    1. LinearLayout: 뷰를 일렬로 배치하는 레이아웃
    2. 화면 너비/높이에 맞춤
    LinearLayout이 수직(vertical) 방향으로 4개의 뷰를 쌓아올린 구조

    -->

    <!--
    @+id : 존재하지 않는 id면 새로 만들어라

    -->
    <TextView
        android:id="@+id/mainTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SPGP 2025"
        android:textColor="#8E0404"
        android:textSize="30sp"
    />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Main Button"
        android:id="@+id/mainButton"
        android:onClick="onMainButtonClick"
        />
    <TextView
        android:id="@+id/subTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="This is a Description"
        android:textColor="#0518A6"
        android:textSize="26sp"
        />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Push Me"
        android:id="@+id/pushMeButton"
        android:onClick="onPushMeClick"
        />

</LinearLayout>